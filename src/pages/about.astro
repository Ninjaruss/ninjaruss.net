---
// There is deliberately no About page: the answer to "who is this?" is the
// current identity declaration in the journal — learn about me through the
// stuff I do. When a newer declaration note is written, repoint this redirect
// at it; /about then becomes a timeline of declarations via git history.
return Astro.redirect('/notes/i-am-ninjaruss', 301);
---
